
avro.subjects.yaml = "schemas.yaml"

avro.files.path = "avro/"

schema.registry {
  url = "http://notused:8081"
  capacity = 20
}

schema.registry.config {

  basic.auth.credentials.source = NOT_USED
  schema.registry.basic.auth.user.info = "fake-api-key:fake-secretkey"
}